#include <iostream>
#include <vector>

int main()
{
    std::vector<int> sampleVector = std::vector<int>();
    std::string msg = "The size of a vector is " + std::to_string(sizeof(sampleVector));
    std::cout << "";
    return 0;
}